# AI 설정 (선택 — 없으면 데모 모드로 동작)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
MODEL_NAME=gpt-4o-mini
TEMPERATURE=0.3
MAX_TOKENS=1024

# 인증 (선택 — 없으면 인증 없이 동작)
API_KEY=

# CORS (쉼표 구분, 기본: localhost)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Sentry 에러 트래킹 (선택)
SENTRY_DSN=

# 로깅
LOG_LEVEL=INFO
